-- displays the nunmber of records with id = 89 in the table first_table of the database hbtn_0c_0 in your MySql server.
-- The database name will be passe as an argument of mysql command

SELECT count(*) FROM first_table WHERE id=89;
